 Flora-Evaluation 模块化架构设计方案

  一、整体架构图

  ┌─────────────────────────────────────────────────────────────────┐
  │                         输入层 (Input Layer)                      │
  │                    Swagger/OpenAPI Documents                      │
  └────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                  Stage 1: API 规范化 (Normalization)             │
  │                      api_normalization/                           │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ - SwaggerParser: 解析 Swagger/OpenAPI                    │   │
  │  │ - SemanticClusterer: 语义聚类                            │   │
  │  │ - CapabilityExtractor: 提取能力模型                      │   │
  │  │ - EntityClusterer: 实体聚类                              │   │
  │  │ - LLMClusterRefiner: LLM 精炼聚类                        │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │  输出: capabilities.json (标准化的 API 能力模型)                 │
  └────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                  Stage 2: API 拓扑构建 (Topology)                │
  │                        api_topology/                              │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ - GraphBuilder: 构建依赖图 (Neo4j)                       │   │
  │  │ - EntityDependencyInferrer: 实体依赖推断                 │   │
  │  │ - LLMEntityInferrer: LLM 实体关系推断                    │   │
  │  │ - FieldMatcher: 字段匹配器                               │   │
  │  │ - PathFinder: 路径查找                                   │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │  输出: Neo4j Graph (API 依赖关系图)                              │
  └────────────┬────────────────────────────────┬────────────────────┘
               │                                │
               │                                │
      ┌────────▼────────┐              ┌────────▼────────┐
      │   分支 A: 构建   │              │   分支 B: 测试   │
      │   自动化系统     │              │   数据生成       │
      └────────┬────────┘              └────────┬────────┘
               │                                │
               │                                │
  ┌────────────▼──────────────┐    ┌───────────▼──────────────┐
  │ Stage 3A: 数据库关系构建   │    │ Stage 3B: 场景生成       │
  │      ddl_entimap/         │    │  scenario_generation/    │
  │ ┌────────────────────┐    │    │ ┌──────────────────┐     │
  │ │ - MetadataProfiler │    │    │ │ - PathGenerator  │     │
  │ │ - SemanticAligner  │    │    │ │ - ScenarioGen    │     │
  │ │ - KnowledgeExporter│    │    │ │ - ScenarioValid  │     │
  │ └────────────────────┘    │    │ └──────────────────┘     │
  │ 输出: entity_mapping.json │    │ 输出: scenarios.json     │
  └────────────┬──────────────┘    └───────────┬──────────────┘
               │                                │
               ▼                                ▼
  ┌────────────────────────────┐    ┌───────────────────────────┐
  │ Stage 4A: Agent 自动构建    │    │ Stage 4B: Mock 服务       │
  │   auto_agents_build/       │    │   stateful_mock/          │
  │ ┌────────────────────┐     │    │ ┌──────────────────┐      │
  │ │ - RoleAlignment    │     │    │ │ - MockServer     │      │
  │ │ - OrgFusion        │     │    │ │ - StateManager   │      │
  │ │ - ArtifactGen      │     │    │ │ - MockService    │      │
  │ │ - Orchestrator     │     │    │ └──────────────────┘      │
  │ └────────────────────┘     │    │ 输出: Mock API Server     │
  │ 输出: Agent System → Neo4j │    └───────────┬───────────────┘
  └────────────┬───────────────┘                │
               │                                │
               │                                │
               └────────────┬───────────────────┘
                            │
                            ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                  Stage 5: 评估与测试 (Evaluation)                │
  │                      coop_eval_actual/                            │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ - 连接 Neo4j 获取自动构建的 Agent 系统                    │   │
  │  │ - 使用 scenarios.json 作为测试用例                        │   │
  │  │ - 使用 stateful_mock 作为 API 环境                        │   │
  │  │ - 执行测试并收集结果                                      │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │  输出: evaluation_results.json                                   │
  └────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
   Stage 6: 自动化调优（使用 automatic_prompt）

  架构更新

  ┌─────────────────────────────────────────────────────────────────┐
  │              Stage 6: 自动化调优 (Optimization)                  │
  │                    automatic_prompt/ (已有模块)                   │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ API Server (api_server.py) - FastAPI REST API           │   │
  │  │ - POST /optimize: 创建优化任务                           │   │
  │  │ - GET /status/{id}: 查询任务状态                         │   │
  │  │ - GET /result/{id}: 获取优化结果                         │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ Core Components:                                          │   │
  │  │ - optimizer.py: PromptOptimizer (主优化器)               │   │
  │  │ - evaluator.py: Evaluator (评估器)                       │   │
  │  │ - generator.py: CandidateGenerator (候选生成器)          │   │
  │  │ - selector.py: Selector (选择器)                         │   │
  │  │ - data_synthesis.py: DataSynthesizer (数据合成)          │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │  输出: optimized_prompts.json (优化后的 Agent Prompts)           │
  └─────────────────────────────────────────────────────────────────┘


  二、模块化设计原则

  1. 接口标准化 (Interface Standardization)

  每个模块定义清晰的输入/输出接口：

  # 标准接口定义
  class ModuleInterface:
      def process(self, input_data: Dict) -> Dict:
          """
          标准处理接口
          Args:
              input_data: 标准化的输入数据
          Returns:
              标准化的输出数据
          """
          pass

      def validate_input(self, input_data: Dict) -> bool:
          """验证输入数据格式"""
          pass

      def get_metadata(self) -> Dict:
          """返回模块元数据（版本、能力等）"""
          pass

  2. 数据格式标准化

  定义统一的数据交换格式：

  # common/schemas.py
  from pydantic import BaseModel
  from typing import List, Dict, Optional

  class APICapability(BaseModel):
      """API 能力模型 - Stage 1 输出"""
      id: str
      name: str
      resource: str
      apis: List[Dict]
      unified_schema: Dict
      lifecycle: Dict

  class TopologyGraph(BaseModel):
      """拓扑图模型 - Stage 2 输出"""
      apis: List[Dict]
      dependencies: List[Dict]
      entities: List[Dict]
      metadata: Dict

  class EntityMapping(BaseModel):
      """实体映射模型 - Stage 3A 输出"""
      entity_name: str
      tables: List[Dict]
      field_mappings: List[Dict]
      golden_sql: List[Dict]

  class TestScenario(BaseModel):
      """测试场景模型 - Stage 3B 输出"""
      scenario_id: str
      api_path: List[str]
      parameter_flow: Dict
      expected_outcome: str

  class AgentSystem(BaseModel):
      """Agent 系统模型 - Stage 4A 输出"""
      agents: List[Dict]
      org_blueprint: Dict
      prompts: Dict
      manifest: Dict

  class EvaluationResult(BaseModel):
      """评估结果模型 - Stage 5 输出"""
      test_id: str
      success: bool
      metrics: Dict
      errors: List[str]

  3. 配置驱动 (Configuration-Driven)

  使用统一的配置文件管理整个流程：

  # config/pipeline_config.yaml
  pipeline:
    name: "Flora-Evaluation-Pipeline"
    version: "1.0.0"

  stages:
    # Stage 1: API 规范化
    normalization:
      module: "api_normalization.NormalizationService"
      enabled: true
      config:
        use_hdbscan: true
        use_prance: true
        enable_evaluation: true
      input:
        type: "file"
        path: "input/swagger.json"
      output:
        type: "file"
        path: "output/stage1/capabilities.json"

    # Stage 2: 拓扑构建
    topology:
      module: "api_topology.TopologyService"
      enabled: true
      config:
        neo4j_uri: "bolt://localhost:7687"
        neo4j_user: "neo4j"
        neo4j_password: "${NEO4J_PASSWORD}"
        use_entity_inference: true
        llm_client: "${LLM_CLIENT}"
      input:
        type: "file"
        path: "output/stage1/capabilities.json"
      output:
        type: "neo4j"
        database: "flora_topology"

    # Stage 3A: 数据库映射
    database_mapping:
      module: "ddl_entimap.EntiMapEngine"
      enabled: true
      config:
        db_url: "${DATABASE_URL}"
        api_key: "${DASHSCOPE_API_KEY}"
        base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
      input:
        type: "file"
        path: "output/stage1/capabilities.json"
      output:
        type: "file"
        path: "output/stage3a/entity_mapping.json"

    # Stage 3B: 场景生成
    scenario_generation:
      module: "scenario_generation.ScenarioGenerationService"
      enabled: true
      config:
        llm_client: "${LLM_CLIENT}"
        max_paths: 10
        max_path_length: 6
      input:
        type: "neo4j"
        database: "flora_topology"
      output:
        type: "file"
        path: "output/stage3b/scenarios.json"

    # Stage 4A: Agent 构建
    agent_build:
      module: "auto_agents_build.PipelineOrchestrator"
      enabled: true
      config:
        llm_provider: "openai"
        output_to_neo4j: true
      input:
        type: "multiple"
        sources:
          - "output/stage1/capabilities.json"
          - "output/stage3a/entity_mapping.json"
      output:
        type: "neo4j"
        database: "flora_agents"

    # Stage 4B: Mock 服务
    mock_service:
      module: "stateful_mock.MockService"
      enabled: true
      config:
        port: 8000
        db_path: "output/stage4b/mock_state.db"
      input:
        type: "file"
        path: "output/stage1/capabilities.json"
      output:
        type: "service"
        endpoint: "http://localhost:8000"

    # Stage 5: 评估测试
    evaluation:
      module: "coop_eval_actual.EvaluationRunner"
      enabled: true
      config:
        neo4j_uri: "bolt://localhost:7687"
        mock_endpoint: "http://localhost:8000"
      input:
        type: "multiple"
        sources:
          - "output/stage3b/scenarios.json"
          - "neo4j://flora_agents"
      output:
        type: "file"
        path: "output/stage5/evaluation_results.json"

    # Stage 6: 自动调优
    optimization:
      module: "optimization.PromptOptimizer"
      enabled: true
      config:
        api_endpoint: "${OPTIMIZATION_API}"
        max_iterations: 5
      input:
        type: "file"
        path: "output/stage5/evaluation_results.json"
      output:
        type: "feedback"
        target_stage: "agent_build"

  # 模块替换配置
  module_registry:
    # 可替换的模块
    normalization:
      default: "api_normalization.NormalizationService"
      alternatives:
        - "custom_normalization.CustomNormalizer"

    topology:
      default: "api_topology.TopologyService"
      alternatives:
        - "graph_topology.GraphService"

    llm_provider:
      default: "openai"
      alternatives:
        - "anthropic"
        - "qwen"
        - "deepseek"

    vector_store:
      default: "neo4j"
      alternatives:
        - "milvus"
        - "chroma"
        - "faiss"

  三、核心组件设计

  1. Pipeline Orchestrator (流程编排器)

  # core/pipeline_orchestrator.py
  from typing import Dict, List, Optional
  import yaml
  from pathlib import Path

  class PipelineOrchestrator:
      """
      流程编排器 - 负责协调所有模块的执行
      """

      def __init__(self, config_path: str):
          self.config = self._load_config(config_path)
          self.modules = {}
          self.results = {}

      def _load_config(self, config_path: str) -> Dict:
          """加载配置文件"""
          with open(config_path, 'r', encoding='utf-8') as f:
              return yaml.safe_load(f)

      def register_module(self, stage_name: str, module_instance):
          """注册模块"""
          self.modules[stage_name] = module_instance

      def run_pipeline(self, start_stage: Optional[str] = None,
                       end_stage: Optional[str] = None):
          """
          运行完整流程或部分流程
          Args:
              start_stage: 起始阶段（None 表示从头开始）
              end_stage: 结束阶段（None 表示运行到最后）
          """
          stages = self.config['stages']
          stage_names = list(stages.keys())

          # 确定执行范围
          start_idx = stage_names.index(start_stage) if start_stage else 0
          end_idx = stage_names.index(end_stage) + 1 if end_stage else len(stage_names)

          # 执行各阶段
          for stage_name in stage_names[start_idx:end_idx]:
              stage_config = stages[stage_name]

              if not stage_config.get('enabled', True):
                  print(f"[SKIP] Stage {stage_name} is disabled")
                  continue

              print(f"[RUN] Stage {stage_name}")
              result = self._execute_stage(stage_name, stage_config)
              self.results[stage_name] = result

          return self.results

      def _execute_stage(self, stage_name: str, stage_config: Dict):
          """执行单个阶段"""
          # 加载输入数据
          input_data = self._load_input(stage_config['input'])

          # 获取模块实例
          module = self.modules.get(stage_name)
          if not module:
              module = self._load_module(stage_config['module'])
              self.modules[stage_name] = module

          # 执行处理
          result = module.process(input_data, stage_config['config'])

          # 保存输出
          self._save_output(result, stage_config['output'])

          return result

      def _load_input(self, input_config: Dict):
          """加载输入数据"""
          input_type = input_config['type']

          if input_type == 'file':
              return self._load_from_file(input_config['path'])
          elif input_type == 'neo4j':
              return self._load_from_neo4j(input_config['database'])
          elif input_type == 'multiple':
              return {
                  src: self._load_input({'type': 'file', 'path': src})
                  for src in input_config['sources']
              }
          else:
              raise ValueError(f"Unknown input type: {input_type}")

      def _save_output(self, result, output_config: Dict):
          """保存输出数据"""
          output_type = output_config['type']

          if output_type == 'file':
              self._save_to_file(result, output_config['path'])
          elif output_type == 'neo4j':
              self._save_to_neo4j(result, output_config['database'])
          elif output_type == 'service':
              # 服务类型不需要保存
              pass
          elif output_type == 'feedback':
              # 反馈到指定阶段
              self._send_feedback(result, output_config['target_stage'])

  2. Module Adapter (模块适配器)

  # core/module_adapter.py
  from abc import ABC, abstractmethod
  from typing import Dict, Any

  class ModuleAdapter(ABC):
      """
      模块适配器基类 - 所有模块都需要实现此接口
      """

      @abstractmethod
      def process(self, input_data: Dict, config: Dict) -> Dict:
          """
          处理数据
          Args:
              input_data: 输入数据
              config: 模块配置
          Returns:
              处理结果
          """
          pass

      @abstractmethod
      def validate_input(self, input_data: Dict) -> bool:
          """验证输入数据"""
          pass

      @abstractmethod
      def get_metadata(self) -> Dict:
          """获取模块元数据"""
          pass


  # 为现有模块创建适配器
  class NormalizationAdapter(ModuleAdapter):
      """API 规范化模块适配器"""

      def __init__(self):
          from api_normalization import NormalizationService
          self.service = None

      def process(self, input_data: Dict, config: Dict) -> Dict:
          from api_normalization import NormalizationService

          if not self.service:
              self.service = NormalizationService(**config)

          # 适配输入格式
          swagger_path = input_data.get('path') or input_data

          # 执行处理
          result = self.service.normalize_swagger(swagger_path)

          # 适配输出格式
          return {
              'capabilities': result['capabilities'],
              'statistics': result['statistics'],
              'metadata': {
                  'stage': 'normalization',
                  'version': '1.0.0'
              }
          }

      def validate_input(self, input_data: Dict) -> bool:
          # 验证输入是否为有效的 Swagger 文件路径
          return isinstance(input_data, (str, dict))

      def get_metadata(self) -> Dict:
          return {
              'name': 'API Normalization',
              'version': '1.0.0',
              'description': 'Normalize Swagger/OpenAPI documents',
              'input_format': 'swagger_json',
              'output_format': 'capabilities_json'
          }


  class TopologyAdapter(ModuleAdapter):
      """API 拓扑模块适配器"""

      def __init__(self):
          self.service = None

      def process(self, input_data: Dict, config: Dict) -> Dict:
          from api_topology import TopologyService

          if not self.service:
              self.service = TopologyService(**config)

          # 提取 capabilities
          capabilities = input_data.get('capabilities', [])

          # 构建图
          result = self.service.build_graph(capabilities)

          return {
              'graph_stats': result,
              'metadata': {
                  'stage': 'topology',
                  'version': '1.0.0'
              }
          }

      def validate_input(self, input_data: Dict) -> bool:
          return 'capabilities' in input_data

      def get_metadata(self) -> Dict:
          return {
              'name': 'API Topology',
              'version': '1.0.0',
              'description': 'Build API dependency graph',
              'input_format': 'capabilities_json',
              'output_format': 'neo4j_graph'
          }

  3. Data Converter (数据转换器)

  # core/data_converter.py
  from typing import Dict, Any

  class DataConverter:
      """
      数据转换器 - 负责不同模块间的数据格式转换
      """

      @staticmethod
      def convert(data: Dict, from_format: str, to_format: str) -> Dict:
          """
          转换数据格式
          Args:
              data: 原始数据
              from_format: 源格式
              to_format: 目标格式
          Returns:
              转换后的数据
          """
          converter_key = f"{from_format}_to_{to_format}"
          converter = getattr(DataConverter, converter_key, None)

          if not converter:
              raise ValueError(f"No converter found for {converter_key}")

          return converter(data)

      @staticmethod
      def capabilities_to_topology(data: Dict) -> Dict:
          """将 capabilities 格式转换为 topology 输入格式"""
          return {
              'capabilities': data.get('capabilities', []),
              'metadata': data.get('metadata', {})
          }

      @staticmethod
      def topology_to_scenarios(data: Dict) -> Dict:
          """将 topology 格式转换为 scenarios 输入格式"""
          # 从 Neo4j 提取数据的逻辑
          pass

  四、替换策略

  1. 模块替换机制

  # core/module_registry.py
  class ModuleRegistry:
      """
      模块注册表 - 管理可替换的模块
      """

      def __init__(self):
          self.registry = {}

      def register(self, module_type: str, module_name: str, module_class):
          """注册模块"""
          if module_type not in self.registry:
              self.registry[module_type] = {}
          self.registry[module_type][module_name] = module_class

      def get_module(self, module_type: str, module_name: str = 'default'):
          """获取模块"""
          return self.registry.get(module_type, {}).get(module_name)

      def list_alternatives(self, module_type: str) -> list:
          """列出可用的替代模块"""
          return list(self.registry.get(module_type, {}).keys())


  # 使用示例
  registry = ModuleRegistry()

  # 注册默认模块
  registry.register('normalization', 'default', NormalizationAdapter)
  registry.register('topology', 'default', TopologyAdapter)

  # 注册替代模块
  registry.register('normalization', 'custom', CustomNormalizationAdapter)
  registry.register('topology', 'graph_based', GraphTopologyAdapter)

  # 获取模块
  module = registry.get_module('normalization', 'default')

  2. LLM 提供商替换

  # core/llm_factory.py
  from typing import Optional
  from openai import OpenAI

  class LLMFactory:
      """
      LLM 工厂 - 支持多种 LLM 提供商
      """

      @staticmethod
      def create_client(provider: str, api_key: str, **kwargs):
          """
          创建 LLM 客户端
          Args:
              provider: 提供商名称 (openai/qwen/deepseek/claude)
              api_key: API 密钥
              **kwargs: 其他配置
          """
          if provider == 'openai':
              return OpenAI(api_key=api_key)

          elif provider == 'qwen':
              return OpenAI(
                  api_key=api_key,
                  base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
              )

          elif provider == 'deepseek':
              return OpenAI(
                  api_key=api_key,
                  base_url="https://api.deepseek.com/v1"
              )

          elif provider == 'claude':
              # 使用 Anthropic SDK
              from anthropic import Anthropic
              return Anthropic(api_key=api_key)

          else:
              raise ValueError(f"Unknown LLM provider: {provider}")

  3. 存储后端替换

  # core/storage_factory.py
  class StorageFactory:
      """
      存储工厂 - 支持多种存储后端
      """

      @staticmethod
      def create_graph_store(store_type: str, **config):
          """
          创建图存储
          Args:
              store_type: 存储类型 (neo4j/milvus/chroma)
              **config: 配置参数
          """
          if store_type == 'neo4j':
              from neo4j import GraphDatabase
              return GraphDatabase.driver(
                  config['uri'],
                  auth=(config['user'], config['password'])
              )

          elif store_type == 'milvus':
              from pymilvus import connections
              connections.connect(**config)
              return connections

          elif store_type == 'chroma':
              import chromadb
              return chromadb.Client()

          else:
              raise ValueError(f"Unknown storage type: {store_type}")

  五、使用示例

  1. 完整流程执行

  # main.py
  from core.pipeline_orchestrator import PipelineOrchestrator
  from core.module_adapter import (
      NormalizationAdapter,
      TopologyAdapter,
      # ... 其他适配器
  )

  # 创建编排器
  orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')

  # 注册模块
  orchestrator.register_module('normalization', NormalizationAdapter())
  orchestrator.register_module('topology', TopologyAdapter())
  # ... 注册其他模块

  # 运行完整流程
  results = orchestrator.run_pipeline()

  print(f"Pipeline completed with {len(results)} stages")

  2. 部分流程执行

  # 只执行 Stage 1-3
  results = orchestrator.run_pipeline(
      start_stage='normalization',
      end_stage='scenario_generation'
  )

  # 从 Stage 3 开始执行
  results = orchestrator.run_pipeline(
      start_stage='scenario_generation'
  )

  3. 模块替换

  # 使用自定义的规范化模块
  from custom_modules import CustomNormalizationAdapter

  orchestrator.register_module('normalization', CustomNormalizationAdapter())

  # 使用不同的 LLM 提供商
  from core.llm_factory import LLMFactory

  llm_client = LLMFactory.create_client('qwen', api_key='your-key')
  topology_adapter = TopologyAdapter()
  topology_adapter.service.llm_client = llm_client

  orchestrator.register_module('topology', topology_adapter)

  六、目录结构建议

  Flora-evaluation/
  ├── core/                          # 核心框架
  │   ├── __init__.py
  │   ├── pipeline_orchestrator.py   # 流程编排器
  │   ├── module_adapter.py          # 模块适配器基类
  │   ├── module_registry.py         # 模块注册表
  │   ├── data_converter.py          # 数据转换器
  │   ├── llm_factory.py             # LLM 工厂
  │   └── storage_factory.py         # 存储工厂
  │
  ├── common/                        # 共享组件
  │   ├── __init__.py
  │   ├── schemas.py                 # 数据模型定义
  │   ├── validators.py              # 数据验证器
  │   └── utils.py                   # 工具函数
  │
  ├── adapters/                      # 模块适配器实现
  │   ├── __init__.py
  │   ├── normalization_adapter.py
  │   ├── topology_adapter.py
  │   ├── entimap_adapter.py
  │   ├── scenario_adapter.py
  │   ├── agent_build_adapter.py
  │   ├── mock_adapter.py
  │   └── evaluation_adapter.py
      ├── optimization_adapter.py  # 优化模块适配器（新增）
  │
  ├── config/                        # 配置文件
  │   ├── pipeline_config.yaml       # 流程配置
  │   ├── module_config.yaml         # 模块配置
  │   └── env.yaml                   # 环境配置
  │
  ├── api_normalization/             # Stage 1: API 规范化
 ├── api_topology/                  # Stage 2: API 拓扑构建
  ├── ddl_entimap/                   # Stage 3A: 数据库映射
  ├── scenario_generation/           # Stage 3B: 场景生成
  ├── auto_agents_build/             # Stage 4A: Agent 构建
  ├── stateful_mock/                 # Stage 4B: Mock 服务
  ├── coop_eval_actual/              # Stage 5: 评估测试
  ├──  ├── automatic_prompt/          # Stage 6: 自动化调优（已有）
  │   ├── api_server.py          # FastAPI 服务
  │   ├── optimizer.py           # 主优化器
  │   ├── evaluator.py           # 评估器
  │   ├── generator.py           # 候选生成器
  │   ├── selector.py            # 选择器
  │   ├── data_synthesis.py      # 数据合成
  │   ├── config.py              # 配置
  │   ├── utils.py               # 工具
  │   └── README.md              # 文档
  │
  ├── examples/                      # 示例代码
  │   ├── full_pipeline.py           # 完整流程示例
  │   ├── partial_pipeline.py        # 部分流程示例
  │   ├── module_replacement.py      # 模块替换示例
  │   └── custom_module.py           # 自定义模块示例
  │
  ├── tests/                         # 测试代码
  │   ├── test_pipeline.py
  │   ├── test_adapters.py
  │   └── test_integration.py
  │
  ├── output/                        # 输出目录
  │   ├── stage1/                    # Stage 1 输出
  │   ├── stage2/                    # Stage 2 输出
  │   ├── stage3a/                   # Stage 3A 输出
  │   ├── stage3b/                   # Stage 3B 输出
  │   ├── stage4a/                   # Stage 4A 输出
  │   ├── stage4b/                   # Stage 4B 输出
  │   ├── stage5/                    # Stage 5 输出
  │   └── stage6/                    # Stage 6 输出
  │
  ├── main.py                        # 主入口
  ├── requirements.txt               # 依赖列表
  └── README.md                      # 项目文档

  七、关键接口定义

  1. Stage 间数据流接口

  # common/schemas.py (续)

  from typing import List, Dict, Optional, Any
  from pydantic import BaseModel, Field
  from datetime import datetime

  # ============ Stage 1 输出 ============
  class APIDefinition(BaseModel):
      """单个 API 定义"""
      operation_id: str
      method: str
      path: str
      summary: Optional[str]
      description: Optional[str]
      parameters: Dict
      responses: Dict
      tags: List[str] = []

  class Capability(BaseModel):
      """能力模型"""
      id: str
      name: str
      resource: str
      apis: List[APIDefinition]
      unified_schema: Dict
      lifecycle: Dict
      connectivity_score: float

  class Stage1Output(BaseModel):
      """Stage 1 标准输出"""
      capabilities: List[Capability]
      statistics: Dict
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 2 输出 ============
  class APIDependency(BaseModel):
      """API 依赖关系"""
      from_api: str
      to_api: str
      score: float
      type: str  # FIELD_MATCH, LLM_SEMANTIC, CRUD_FLOW
      fields: List[str] = []

  class EntityRelation(BaseModel):
      """实体关系"""
      from_entity: str
      to_entity: str
      confidence: float
      inferred_from: str  # LLM_INFERENCE, FIELD_REFERENCE, PATH_HIERARCHY

  class Stage2Output(BaseModel):
      """Stage 2 标准输出"""
      apis: List[APIDefinition]
      dependencies: List[APIDependency]
      entities: List[Dict]
      entity_relations: List[EntityRelation]
      graph_stats: Dict
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 3A 输出 ============
  class FieldMapping(BaseModel):
      """字段映射"""
      db_column: str
      api_field: str
      role: str  # business, technical, hidden_logic
      confidence: float

  class TableMapping(BaseModel):
      """表映射"""
      table_name: str
      entity_name: str
      relation_type: str  # core, association, log, irrelevant
      relation_score: float
      field_mappings: List[FieldMapping]

  class GoldenSQL(BaseModel):
      """Golden SQL"""
      question: str
      sql: str
      entity: str

  class Stage3AOutput(BaseModel):
      """Stage 3A 标准输出"""
      entity_mappings: List[TableMapping]
      golden_sqls: List[GoldenSQL]
      vanna_training_data: Dict
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 3B 输出 ============
  class ParameterBinding(BaseModel):
      """参数绑定"""
      target_field: str
      source_path: str  # e.g., "login.response.token"
      transform: Optional[str] = None

  class TestStep(BaseModel):
      """测试步骤"""
      api: str
      parameters: Dict[str, ParameterBinding]
      expected_status: int = 200

  class TestScenario(BaseModel):
      """测试场景"""
      scenario_id: str
      title: str
      description: str
      scenario_type: str  # normal, exception
      api_path: List[str]
      steps: List[TestStep]
      expected_outcome: str
      validation_score: float

  class Stage3BOutput(BaseModel):
      """Stage 3B 标准输出"""
      scenarios: List[TestScenario]
      paths: List[Dict]
      statistics: Dict
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 4A 输出 ============
  class AgentDefinition(BaseModel):
      """Agent 定义"""
      agent_id: str
      name: str
      role: str
      level: str  # specialist, manager, director
      capabilities: List[str]
      prompt: str
      tools: List[str]

  class OrgBlueprint(BaseModel):
      """组织蓝图"""
      agents: List[AgentDefinition]
      hierarchy: Dict
      access_control: Dict

  class Stage4AOutput(BaseModel):
      """Stage 4A 标准输出"""
      org_blueprint: OrgBlueprint
      role_manifest: Dict
      prompts: Dict[str, str]
      manifest: Dict
      neo4j_location: str  # Neo4j 存储位置
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 4B 输出 ============
  class MockEndpoint(BaseModel):
      """Mock 端点"""
      operation_id: str
      method: str
      path: str
      handler: str

  class Stage4BOutput(BaseModel):
      """Stage 4B 标准输出"""
      service_url: str
      endpoints: List[MockEndpoint]
      state_db_path: str
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 5 输出 ============
  class TestResult(BaseModel):
      """单个测试结果"""
      test_id: str
      scenario_id: str
      success: bool
      execution_time: float
      api_calls: int
      errors: List[str]
      metrics: Dict

  class Stage5Output(BaseModel):
      """Stage 5 标准输出"""
      test_results: List[TestResult]
      summary: Dict
      success_rate: float
      average_execution_time: float
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  # ============ Stage 6 输出 ============
  class OptimizationSuggestion(BaseModel):
      """优化建议"""
      target: str  # agent_id or module_name
      issue: str
      suggestion: str
      priority: str  # high, medium, low
      estimated_impact: float

  class Stage6Output(BaseModel):
      """Stage 6 标准输出"""
      suggestions: List[OptimizationSuggestion]
      optimized_prompts: Dict[str, str]
      performance_improvement: Dict
      should_rebuild: bool
      metadata: Dict
      timestamp: datetime = Field(default_factory=datetime.now)

  2. 模块适配器完整实现

  # adapters/scenario_adapter.py
  from core.module_adapter import ModuleAdapter
  from common.schemas import Stage2Output, Stage3BOutput
  from typing import Dict

  class ScenarioAdapter(ModuleAdapter):
      """场景生成模块适配器"""

      def __init__(self):
          self.service = None
          self.path_generator = None

      def process(self, input_data: Dict, config: Dict) -> Stage3BOutput:
          from scenario_generation import ScenarioGenerationService
          from scenario_generation.path_generator import PathGenerator
          from openai import OpenAI

          # 初始化服务
          if not self.service:
              self.service = ScenarioGenerationService()

          # 初始化 LLM 客户端（如果配置了）
          llm_client = None
          if config.get('llm_client'):
              llm_client = OpenAI(**config['llm_client'])

          if not self.path_generator:
              self.path_generator = PathGenerator(llm_client=llm_client)

          # 从 Neo4j 或输入数据提取拓扑信息
          topology_data = self._extract_topology(input_data, config)

          # 生成路径
          paths = self.path_generator.generate_paths(
              topology_data=topology_data,
              max_paths=config.get('max_paths', 10),
              max_path_length=config.get('max_path_length', 6),
              min_path_length=config.get('min_path_length', 2)
          )

          # 为每条路径生成场景
          all_scenarios = []
          api_details = {api['operation_id']: api for api in topology_data['apis']}

          for path_info in paths:
              scenarios = self.service.generate_scenarios(
                  api_path=path_info['path'],
                  api_details=api_details,
                  parameter_flow=path_info.get('parameter_flow', {}),
                  scenario_types=config.get('scenario_types', ['normal', 'exception']),
                  count_per_type=config.get('count_per_type', 2)
              )
              all_scenarios.extend(scenarios)

          # 构建输出
          return Stage3BOutput(
              scenarios=all_scenarios,
              paths=paths,
              statistics={
                  'total_paths': len(paths),
                  'total_scenarios': len(all_scenarios),
                  'valid_scenarios': sum(1 for s in all_scenarios if s.get('validation', {}).get('is_valid', True))
              },
              metadata={
                  'stage': 'scenario_generation',
                  'version': '1.0.0',
                  'config': config
              }
          )

      def _extract_topology(self, input_data: Dict, config: Dict) -> Dict:
          """从输入数据或 Neo4j 提取拓扑信息"""
          if 'apis' in input_data and 'dependencies' in input_data:
              # 直接从输入数据获取
              return input_data

          # 从 Neo4j 提取
          from neo4j import GraphDatabase

          driver = GraphDatabase.driver(
              config['neo4j_uri'],
              auth=(config['neo4j_user'], config['neo4j_password'])
          )

          with driver.session() as session:
              # 获取 APIs
              apis_result = session.run("""
                  MATCH (a:API)
                  RETURN a.operation_id as operation_id,
                         a.method as method,
                         a.path as path,
                         a.summary as summary,
                         a.parameters as parameters,
                         a.responses as responses,
                         a.entity as entity
              """)
              apis = [dict(record) for record in apis_result]

              # 获取依赖关系
              deps_result = session.run("""
                  MATCH (a1:API)-[r:DEPENDS_ON]->(a2:API)
                  RETURN a1.operation_id as from,
                         a2.operation_id as to,
                         r.score as score,
                         r.type as type
              """)
              dependencies = [dict(record) for record in deps_result]

              # 获取实体
              entities_result = session.run("""
                  MATCH (e:Entity)
                  OPTIONAL MATCH (a:API)-[:BELONGS_TO]->(e)
                  RETURN e.name as name,
                         collect(a.operation_id) as apis
              """)
              entities = [dict(record) for record in entities_result]

          driver.close()

          return {
              'apis': apis,
              'dependencies': dependencies,
              'entities': entities
          }

      def validate_input(self, input_data: Dict) -> bool:
          # 验证是否包含必要的拓扑信息或 Neo4j 配置
          has_topology = 'apis' in input_data and 'dependencies' in input_data
          has_neo4j_config = 'neo4j_uri' in input_data
          return has_topology or has_neo4j_config

      def get_metadata(self) -> Dict:
          return {
              'name': 'Scenario Generation',
              'version': '1.0.0',
              'description': 'Generate test scenarios from API topology',
              'input_format': 'topology_graph',
              'output_format': 'test_scenarios'
          }


  # adapters/agent_build_adapter.py
  class AgentBuildAdapter(ModuleAdapter):
      """Agent 构建模块适配器"""

      def __init__(self):
          self.orchestrator = None

      def process(self, input_data: Dict, config: Dict) -> Stage4AOutput:
          from auto_agents_build.orchestrator import PipelineOrchestrator

          if not self.orchestrator:
              self.orchestrator = PipelineOrchestrator()

          # 提取 API 能力
          capabilities = input_data.get('capabilities', [])
          if isinstance(input_data, dict) and 'sources' in input_data:
              # 多源输入
              capabilities = input_data['sources'][0].get('capabilities', [])

          # 转换为 API 能力列表
          api_capabilities = self._convert_to_api_capabilities(capabilities)

          # 运行构建流程
          result = self.orchestrator.run_pipeline(
              api_capabilities,
              output_dir=config.get('output_dir', './output/stage4a')
          )

          # 如果配置了输出到 Neo4j，则保存
          if config.get('output_to_neo4j', False):
              neo4j_location = self._save_to_neo4j(result, config)
          else:
              neo4j_location = None

          return Stage4AOutput(
              org_blueprint=result['org_blueprint'],
              role_manifest=result['role_manifest'],
              prompts=result['prompts'],
              manifest=result['manifest'],
              neo4j_location=neo4j_location,
              metadata={
                  'stage': 'agent_build',
                  'version': '1.0.0',
                  'config': config
              }
          )

      def _convert_to_api_capabilities(self, capabilities: List) -> List[Dict]:
          """转换能力格式为 Agent 构建所需格式"""
          api_capabilities = []

          for cap in capabilities:
              for api in cap.get('apis', []):
                  api_capabilities.append({
                      'id': api.get('operation_id'),
                      'name': api.get('summary', api.get('operation_id')),
                      'description': api.get('description', ''),
                      'method': api.get('method'),
                      'path': api.get('path'),
                      'tags': cap.get('tags', [cap.get('resource', 'unknown')])
                  })

          return api_capabilities

      def _save_to_neo4j(self, result: Dict, config: Dict) -> str:
          """保存 Agent 系统到 Neo4j"""
          from neo4j import GraphDatabase

          driver = GraphDatabase.driver(
              config['neo4j_uri'],
              auth=(config['neo4j_user'], config['neo4j_password'])
          )

          with driver.session() as session:
              # 清空现有数据
              session.run("MATCH (n:Agent) DETACH DELETE n")

              # 创建 Agent 节点
              for agent in result['org_blueprint']['agents']:
                  session.run("""
                      CREATE (a:Agent {
                          agent_id: $agent_id,
                          name: $name,
                          role: $role,
                          level: $level,
                          prompt: $prompt
                      })
                  """, agent)

              # 创建层级关系
              for relation in result['org_blueprint']['hierarchy'].get('relations', []):
                  session.run("""
                      MATCH (a1:Agent {agent_id: $from})
                      MATCH (a2:Agent {agent_id: $to})
                      CREATE (a1)-[:MANAGES]->(a2)
                  """, relation)

          driver.close()

          return f"{config['neo4j_uri']}/flora_agents"

      def validate_input(self, input_data: Dict) -> bool:
          return 'capabilities' in input_data or 'sources' in input_data

      def get_metadata(self) -> Dict:
          return {
              'name': 'Agent Build',
              'version': '1.0.0',
              'description': 'Build agent system from API capabilities',
              'input_format': 'capabilities_json',
              'output_format': 'agent_system'
          }


  # adapters/evaluation_adapter.py
  class EvaluationAdapter(ModuleAdapter):
      """评估模块适配器"""

      def __init__(self):
          self.runner = None

      def process(self, input_data: Dict, config: Dict) -> Stage5Output:
          from coop_eval_actual.evaluation_runner import EvaluationRunner

          if not self.runner:
              self.runner = EvaluationRunner(
                  neo4j_uri=config['neo4j_uri'],
                  neo4j_user=config.get('neo4j_user', 'neo4j'),
                  neo4j_password=config.get('neo4j_password', 'password'),
                  mock_endpoint=config['mock_endpoint']
              )

          # 提取测试场景
          scenarios = input_data.get('scenarios', [])
          if 'sources' in input_data:
              scenarios = input_data['sources'][0].get('scenarios', [])

          # 运行评估
          results = self.runner.run_evaluation(scenarios)

          # 计算统计信息
          total_tests = len(results)
          successful_tests = sum(1 for r in results if r['success'])
          total_time = sum(r['execution_time'] for r in results)

          return Stage5Output(
              test_results=results,
              summary={
                  'total_tests': total_tests,
                  'successful_tests': successful_tests,
                  'failed_tests': total_tests - successful_tests
              },
              success_rate=successful_tests / total_tests if total_tests > 0 else 0,
              average_execution_time=total_time / total_tests if total_tests > 0 else 0,
              metadata={
                  'stage': 'evaluation',
                  'version': '1.0.0',
                  'config': config
              }
          )

      def validate_input(self, input_data: Dict) -> bool:
          return 'scenarios' in input_data or 'sources' in input_data

      def get_metadata(self) -> Dict:
          return {
              'name': 'Evaluation',
              'version': '1.0.0',
              'description': 'Evaluate agent system with test scenarios',
              'input_format': 'test_scenarios',
              'output_format': 'evaluation_results'
          }

  八、优化模块设计（新增 Stage 6）

  集成方式

  # adapters/optimization_adapter.py
  """
  优化模块适配器 - 集成 automatic_prompt 服务
  """

  from core.module_adapter import ModuleAdapter
  from common.schemas import Stage5Output, Stage6Output, OptimizationSuggestion
  from typing import Dict, List
  import requests
  import time

  class OptimizationAdapter(ModuleAdapter):
      """
      优化模块适配器 - 调用 automatic_prompt API 服务
      """

      def __init__(self):
          self.api_base_url = None
          self.task_ids = {}

      def process(self, input_data: Dict, config: Dict) -> Stage6Output:
          """
          处理优化请求

          Args:
              input_data: Stage 5 的评估结果
              config: 优化配置
                  - api_endpoint: automatic_prompt API 地址
                  - max_iterations: 最大迭代次数
                  - auto_augment_data: 是否自动增强数据
          """
          from common.schemas import Stage5Output

          # 提取评估结果
          evaluation_results = Stage5Output(**input_data)

          # 设置 API 地址
          self.api_base_url = config.get('api_endpoint', 'http://localhost:8100')

          # 分析失败的测试，识别需要优化的 Agent
          agents_to_optimize = self._identify_agents_to_optimize(evaluation_results)

          # 为每个 Agent 创建优化任务
          optimization_tasks = []
          for agent_info in agents_to_optimize:
              task_id = self._create_optimization_task(agent_info, config)
              optimization_tasks.append({
                  'agent_id': agent_info['agent_id'],
                  'task_id': task_id,
                  'current_prompt': agent_info['current_prompt'],
                  'issues': agent_info['issues']
              })

          # 等待所有优化任务完成
          optimized_prompts = {}
          suggestions = []

          for task in optimization_tasks:
              result = self._wait_for_optimization(task['task_id'])

              if result['status'] == 'completed':
                  optimized_prompts[task['agent_id']] = result['best_prompt']

                  # 生成优化建议
                  suggestion = OptimizationSuggestion(
                      target=task['agent_id'],
                      issue=f"准确率低: {', '.join(task['issues'])}",
                      suggestion=f"使用优化后的 Prompt (改进: {result['improvement']:.2%})",
                      priority='high' if result['improvement'] > 0.1 else 'medium',
                      estimated_impact=result['improvement']
                  )
                  suggestions.append(suggestion)

          # 计算性能改进
          performance_improvement = self._estimate_improvement(suggestions)

          # 判断是否需要重新构建
          should_rebuild = any(s.priority == 'high' for s in suggestions)

          return Stage6Output(
              suggestions=suggestions,
              optimized_prompts=optimized_prompts,
              performance_improvement=performance_improvement,
              should_rebuild=should_rebuild,
              metadata={
                  'stage': 'optimization',
                  'version': '1.0.0',
                  'api_endpoint': self.api_base_url,
                  'total_agents_optimized': len(optimized_prompts)
              }
          )

      def _identify_agents_to_optimize(self, evaluation_results: Stage5Output) -> List[Dict]:
          """
          识别需要优化的 Agent

          分析评估结果，找出表现不佳的 Agent
          """
          agents_to_optimize = []

          # 按 Agent 分组统计失败情况
          agent_failures = {}

          for test_result in evaluation_results.test_results:
              if not test_result.success:
                  # 从错误信息中提取 Agent ID（假设错误信息包含 Agent 信息）
                  agent_id = self._extract_agent_from_error(test_result.errors)

                  if agent_id not in agent_failures:
                      agent_failures[agent_id] = {
                          'agent_id': agent_id,
                          'failures': 0,
                          'issues': []
                      }

                  agent_failures[agent_id]['failures'] += 1
                  agent_failures[agent_id]['issues'].extend(test_result.errors)

          # 筛选需要优化的 Agent（失败次数 > 2）
          for agent_id, info in agent_failures.items():
              if info['failures'] > 2:
                  # 获取当前 Prompt
                  current_prompt = self._get_current_prompt(agent_id)

                  agents_to_optimize.append({
                      'agent_id': agent_id,
                      'current_prompt': current_prompt,
                      'failures': info['failures'],
                      'issues': list(set(info['issues']))  # 去重
                  })

          return agents_to_optimize

      def _create_optimization_task(self, agent_info: Dict, config: Dict) -> str:
          """
          调用 automatic_prompt API 创建优化任务
          """
          # 构建优化请求
          request_data = {
              "task_name": f"优化 Agent: {agent_info['agent_id']}",
              "task_description": f"优化 Agent Prompt 以提高准确率",

              # 从失败案例中提取训练样本
              "examples": self._extract_examples_from_failures(agent_info['issues']),

              # 使用测试场景作为验证数据
              "validation_data": self._extract_validation_data(agent_info['agent_id']),

              # 当前 Prompt 作为初始提示词
              "initial_prompts": [agent_info['current_prompt']],

              # 优化配置
              "max_iterations": config.get('max_iterations', 10),
              "num_candidates": config.get('num_candidates', 5),
              "top_k": config.get('top_k', 3),
              "early_stop_threshold": config.get('early_stop_threshold', 0.90),
              "auto_augment_data": config.get('auto_augment_data', True),
              "fast_mode": config.get('fast_mode', False)
          }

          # 调用 API
          response = requests.post(
              f"{self.api_base_url}/optimize",
              json=request_data,
              timeout=30
          )

          if response.status_code == 200:
              result = response.json()
              return result['task_id']
          else:
              raise Exception(f"优化任务创建失败: {response.text}")

      def _wait_for_optimization(self, task_id: str, timeout: int = 600) -> Dict:
          """
          等待优化任务完成

          Args:
              task_id: 任务 ID
              timeout: 超时时间（秒）
          """
          start_time = time.time()

          while time.time() - start_time < timeout:
              # 查询任务状态
              response = requests.get(
                  f"{self.api_base_url}/status/{task_id}",
                  timeout=10
              )

              if response.status_code == 200:
                  result = response.json()

                  if result['status'] == 'completed':
                      return result
                  elif result['status'] == 'failed':
                      raise Exception(f"优化任务失败: {result.get('error')}")

                  # 等待 5 秒后重试
                  time.sleep(5)
              else:
                  raise Exception(f"查询任务状态失败: {response.text}")

          raise TimeoutError(f"优化任务超时: {task_id}")

      def _extract_agent_from_error(self, errors: List[str]) -> str:
          """从错误信息中提取 Agent ID"""
          # 实现逻辑：解析错误信息，提取 Agent ID
          # 例如：错误信息格式为 "Agent[order_manager] failed: ..."
          for error in errors:
              if 'Agent[' in error:
                  start = error.index('Agent[') + 6
                  end = error.index(']', start)
                  return error[start:end]

          return 'unknown_agent'

      def _get_current_prompt(self, agent_id: str) -> str:
          """
          从 Neo4j 获取 Agent 的当前 Prompt
          """
          # 实现逻辑：连接 Neo4j，查询 Agent 的 Prompt
          # 这里返回示例
          return f"You are {agent_id}. Please help users with their tasks."

      def _extract_examples_from_failures(self, issues: List[str]) -> List[Dict]:
          """
          从失败案例中提取训练样本

          将失败的测试转换为训练样本
          """
          examples = []

          # 至少需要 3 个样本
          for i, issue in enumerate(issues[:5]):
              examples.append({
                  "input": f"处理任务: {issue}",
                  "output": "成功完成任务"
              })

          # 如果不足 3 个，添加通用样本
          while len(examples) < 3:
              examples.append({
                  "input": "执行标准任务",
                  "output": "任务完成"
              })

          return examples

      def _extract_validation_data(self, agent_id: str) -> List[Dict]:
          """
          提取验证数据

          从测试场景中提取相关的验证数据
          """
          # 实现逻辑：从 Stage 3B 的场景中提取
          # 这里返回示例
          return [
              {"input": "测试场景 1", "output": "预期结果 1"},
              {"input": "测试场景 2", "output": "预期结果 2"},
              {"input": "测试场景 3", "output": "预期结果 3"}
          ]

      def _estimate_improvement(self, suggestions: List[OptimizationSuggestion]) -> Dict:
          """估算性能改进"""
          total_impact = sum(s.estimated_impact for s in suggestions)

          return {
              'estimated_success_rate_improvement': total_impact,
              'high_priority_count': sum(1 for s in suggestions if s.priority == 'high'),
              'medium_priority_count': sum(1 for s in suggestions if s.priority == 'medium'),
              'low_priority_count': sum(1 for s in suggestions if s.priority == 'low'),
              'total_agents_optimized': len(suggestions)
          }

      def validate_input(self, input_data: Dict) -> bool:
          """验证输入"""
          return 'test_results' in input_data

      def get_metadata(self) -> Dict:
          return {
              'name': 'Automatic Prompt Optimization',
              'version': '1.0.0',
              'description': 'Optimize agent prompts using automatic_prompt service',
              'input_format': 'evaluation_results',
              'output_format': 'optimization_suggestions',
              'api_service': 'automatic_prompt'
          }

  更新后的配置文件

  # config/pipeline_config.yaml

  stages:
    # ... 前面的阶段 ...

    # Stage 6: 自动化调优（使用 automatic_prompt）
    optimization:
      module: "optimization.OptimizationAdapter"
      enabled: true
      config:
        # automatic_prompt API 地址
        api_endpoint: "http://localhost:8100"

        # 优化参数
        max_iterations: 10
        num_candidates: 5
        top_k: 3
        early_stop_threshold: 0.90
        auto_augment_data: true
        fast_mode: false  # 生产环境设为 false

        # 超时设置
        optimization_timeout: 600  # 10分钟

      input:
        type: "file"
        path: "output/stage5/evaluation_results.json"

      output:
        type: "file"
        path: "output/stage6/optimization.json"

  启动 automatic_prompt 服务

  # scripts/start_optimization_service.py
  """
  启动 automatic_prompt 优化服务
  """

  import subprocess
  import time
  import requests
  import sys

  def start_service():
      """启动优化服务"""
      print("🚀 启动 automatic_prompt 优化服务...")

      # 启动 API 服务
      process = subprocess.Popen(
          [sys.executable, "automatic_prompt/api_server.py"],
          stdout=subprocess.PIPE,
          stderr=subprocess.PIPE
      )

      # 等待服务启动
      print("⏳ 等待服务启动...")
      time.sleep(5)

      # 检查服务健康状态
      try:
          response = requests.get("http://localhost:8100/health", timeout=5)
          if response.status_code == 200:
              health = response.json()
              print(f"✅ 服务启动成功!")
              print(f"   状态: {health['status']}")
              print(f"   版本: {health['version']}")
              print(f"   API Key 配置: {'✓' if health['api_key_configured'] else '✗'}")
              print(f"\n📖 API 文档: http://localhost:8100/docs")
              return process
          else:
              print("❌ 服务启动失败")
              process.kill()
              return None
      except Exception as e:
          print(f"❌ 无法连接到服务: {e}")
          process.kill()
          return None

  if __name__ == '__main__':
      process = start_service()

      if process:
          print("\n按 Ctrl+C 停止服务...")
          try:
              process.wait()
          except KeyboardInterrupt:
              print("\n🛑 停止服务...")
              process.kill()

  完整的使用流程

  # examples/full_pipeline_with_optimization.py
  """
  完整流程示例 - 包含自动优化
  """

  import subprocess
  import time
  from core.pipeline_orchestrator import ResilientPipelineOrchestrator
  from adapters import *

  def main():
      # Step 1: 启动 automatic_prompt 服务
      print("=" * 70)
      print("Step 1: 启动优化服务")
      print("=" * 70)

      optimization_service = subprocess.Popen(
          ["python", "automatic_prompt/api_server.py"],
          stdout=subprocess.PIPE,
          stderr=subprocess.PIPE
      )

      # 等待服务启动
      time.sleep(5)

      try:
          # Step 2: 运行完整流程
          print("\n" + "=" * 70)
          print("Step 2: 运行 Flora-Evaluation Pipeline")
          print("=" * 70)

          orchestrator = ResilientPipelineOrchestrator('config/pipeline_config.yaml')

          # 注册所有模块
          orchestrator.register_module('normalization', NormalizationAdapter())
          orchestrator.register_module('topology', TopologyAdapter())
          orchestrator.register_module('database_mapping', EntiMapAdapter())
          orchestrator.register_module('scenario_generation', ScenarioAdapter())
          orchestrator.register_module('agent_build', AgentBuildAdapter())
          orchestrator.register_module('mock_service', MockAdapter())
          orchestrator.register_module('evaluation', EvaluationAdapter())
          orchestrator.register_module('optimization', OptimizationAdapter())  # 新增

          # 运行完整流程
          results = orchestrator.run_pipeline()

          # Step 3: 检查是否需要优化循环
          if results['optimization'].should_rebuild:
              print("\n" + "=" * 70)
              print("Step 3: 执行优化循环")
              print("=" * 70)

              run_optimization_loop(orchestrator, results)

          print("\n✅ 流程完成!")

      finally:
          # 停止优化服务
          print("\n🛑 停止优化服务...")
          optimization_service.kill()

  def run_optimization_loop(orchestrator, initial_results, max_iterations=5):
      """运行优化循环"""
      iteration = 1
      previous_success_rate = initial_results['evaluation'].success_rate

      while iteration <= max_iterations:
          print(f"\n--- 优化迭代 {iteration}/{max_iterations} ---")

          # 应用优化后的 Prompts
          optimized_prompts = initial_results['optimization'].optimized_prompts
          apply_optimized_prompts(optimized_prompts)

          # 重新运行评估和优化
          results = orchestrator.run_pipeline(
              start_stage='agent_build',  # 从 Agent 构建开始
              end_stage='optimization'
          )

          # 检查改进
          current_success_rate = results['evaluation'].success_rate
          improvement = current_success_rate - previous_success_rate

          print(f"成功率: {previous_success_rate:.2%} -> {current_success_rate:.2%}")
          print(f"改进: {improvement:+.2%}")

          if not results['optimization'].should_rebuild:
              print("✅ 优化收敛!")
              break

          if improvement <= 0:
              print("⚠️  无改进，停止优化")
              break

          previous_success_rate = current_success_rate
          initial_results = results
          iteration += 1

  def apply_optimized_prompts(optimized_prompts: Dict[str, str]):
      """
      应用优化后的 Prompts 到 Neo4j
      """
      from neo4j import GraphDatabase
      import os

      driver = GraphDatabase.driver(
          os.getenv('NEO4J_URI', 'bolt://localhost:7687'),
          auth=(os.getenv('NEO4J_USER', 'neo4j'),
                os.getenv('NEO4J_PASSWORD', 'password'))
      )

      with driver.session() as session:
          for agent_id, new_prompt in optimized_prompts.items():
              session.run("""
                  MATCH (a:Agent {agent_id: $agent_id})
                  SET a.prompt = $new_prompt,
                      a.updated_at = datetime(),
                      a.version = coalesce(a.version, 0) + 1
              """, agent_id=agent_id, new_prompt=new_prompt)

              print(f"  ✓ 更新 Agent: {agent_id}")

      driver.close()

  if __name__ == '__main__':
      main()

  九、完整使用示例

  # examples/full_pipeline.py
  """
  完整流程示例 - 从 Swagger 到优化的完整流程
  """

  import os
  from core.pipeline_orchestrator import PipelineOrchestrator
  from adapters import (
      NormalizationAdapter,
      TopologyAdapter,
      EntiMapAdapter,
      ScenarioAdapter,
      AgentBuildAdapter,
      MockAdapter,
      EvaluationAdapter,
      OptimizationAdapter
  )

  def main():
      # 设置环境变量
      os.environ['NEO4J_PASSWORD'] = 'your-password'
      os.environ['DASHSCOPE_API_KEY'] = 'your-api-key'
      os.environ['DATABASE_URL'] = 'mysql://user:pass@host:port/db'

      # 创建编排器
      orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')

      # 注册所有模块
      orchestrator.register_module('normalization', NormalizationAdapter())
      orchestrator.register_module('topology', TopologyAdapter())
      orchestrator.register_module('database_mapping', EntiMapAdapter())
      orchestrator.register_module('scenario_generation', ScenarioAdapter())
      orchestrator.register_module('agent_build', AgentBuildAdapter())
      orchestrator.register_module('mock_service', MockAdapter())
      orchestrator.register_module('evaluation', EvaluationAdapter())
      orchestrator.register_module('optimization', OptimizationAdapter())

      # 运行完整流程
      print("Starting Flora-Evaluation Pipeline...")
      results = orchestrator.run_pipeline()

      # 输出结果摘要
      print("\n=== Pipeline Execution Summary ===")
      for stage_name, result in results.items():
          print(f"\n[{stage_name}]")
          if hasattr(result, 'metadata'):
              print(f"  Timestamp: {result.timestamp}")
              print(f"  Version: {result.metadata.get('version', 'N/A')}")

          # 输出关键指标
          if stage_name == 'normalization':
              print(f"  Capabilities: {len(result.capabilities)}")
          elif stage_name == 'topology':
              print(f"  APIs: {len(result.apis)}")
              print(f"  Dependencies: {len(result.dependencies)}")
          elif stage_name == 'scenario_generation':
              print(f"  Scenarios: {len(result.scenarios)}")
          elif stage_name == 'evaluation':
              print(f"  Success Rate: {result.success_rate:.2%}")
              print(f"  Avg Execution Time: {result.average_execution_time:.2f}s")
          elif stage_name == 'optimization':
              print(f"  Suggestions: {len(result.suggestions)}")
              print(f"  Should Rebuild: {result.should_rebuild}")

      # 如果需要重新构建，触发优化循环
      if results['optimization'].should_rebuild:
          print("\n=== Starting Optimization Loop ===")
          run_optimization_loop(orchestrator, results)

      print("\n=== Pipeline Completed ===")

  def run_optimization_loop(orchestrator, initial_results):
      """运行优化循环"""
      max_iterations = 5
      current_iteration = 1

      while current_iteration <= max_iterations:
          print(f"\n--- Optimization Iteration {current_iteration} ---")

          # 应用优化的 Prompts
          optimized_prompts = initial_results['optimization'].optimized_prompts
          # TODO: 更新 Agent 系统的 Prompts

          # 重新运行评估
          results = orchestrator.run_pipeline(
              start_stage='evaluation',
              end_stage='optimization'
          )

          # 检查是否还需要继续优化
          if not results['optimization'].should_rebuild:
              print("Optimization converged!")
              break

          # 检查是否有改进
          new_success_rate = results['evaluation'].success_rate
          old_success_rate = initial_results['evaluation'].success_rate

          if new_success_rate <= old_success_rate:
              print("No improvement detected, stopping optimization.")
              break

          print(f"Success rate improved: {old_success_rate:.2%} -> {new_success_rate:.2%}")

          initial_results = results
          current_iteration += 1

    if __name__ == '__main__':
      main()


  # examples/partial_pipeline.py
  """
  部分流程示例 - 只运行特定阶段
  """

  from core.pipeline_orchestrator import PipelineOrchestrator
  from adapters import *

  def run_normalization_only():
      """只运行 API 规范化"""
      orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')
      orchestrator.register_module('normalization', NormalizationAdapter())

      results = orchestrator.run_pipeline(
          start_stage='normalization',
          end_stage='normalization'
      )

      print(f"Generated {len(results['normalization'].capabilities)} capabilities")
      return results

  def run_testing_branch():
      """只运行测试分支（3B -> 4B -> 5）"""
      orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')

      # 注册测试分支相关模块
      orchestrator.register_module('scenario_generation', ScenarioAdapter())
      orchestrator.register_module('mock_service', MockAdapter())
      orchestrator.register_module('evaluation', EvaluationAdapter())

      results = orchestrator.run_pipeline(
          start_stage='scenario_generation',
          end_stage='evaluation'
      )

      return results

  def run_build_branch():
      """只运行构建分支（3A -> 4A）"""
      orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')

      orchestrator.register_module('database_mapping', EntiMapAdapter())
      orchestrator.register_module('agent_build', AgentBuildAdapter())

      results = orchestrator.run_pipeline(
          start_stage='database_mapping',
          end_stage='agent_build'
      )

      return results


  # examples/module_replacement.py
  """
  模块替换示例 - 演示如何替换模块
  """

  from core.pipeline_orchestrator import PipelineOrchestrator
  from core.module_adapter import ModuleAdapter
  from typing import Dict

  # 自定义规范化模块
  class CustomNormalizationAdapter(ModuleAdapter):
      """自定义的 API 规范化适配器"""

      def process(self, input_data: Dict, config: Dict) -> Dict:
          print("[Custom] Using custom normalization logic...")

          # 实现自定义逻辑
          # 例如：使用不同的聚类算法、不同的实体识别方法等

          return {
              'capabilities': [],  # 自定义处理结果
              'metadata': {'custom': True}
          }

      def validate_input(self, input_data: Dict) -> bool:
          return True

      def get_metadata(self) -> Dict:
          return {
              'name': 'Custom Normalization',
              'version': '2.0.0',
              'description': 'Custom API normalization with enhanced clustering'
          }

  # 自定义 LLM 提供商
  class CustomLLMAdapter(ModuleAdapter):
      """自定义 LLM 提供商适配器"""

      def __init__(self, provider: str, api_key: str):
          self.provider = provider
          self.api_key = api_key
          self.client = self._create_client()

      def _create_client(self):
          if self.provider == 'custom_llm':
              # 实现自定义 LLM 客户端
              return CustomLLMClient(self.api_key)
          else:
              from openai import OpenAI
              return OpenAI(api_key=self.api_key)

      def process(self, input_data: Dict, config: Dict) -> Dict:
          # 使用自定义 LLM 处理
          pass

  def main():
      orchestrator = PipelineOrchestrator('config/pipeline_config.yaml')

      # 使用自定义规范化模块
      orchestrator.register_module('normalization', CustomNormalizationAdapter())

      # 使用自定义 LLM
      custom_llm = CustomLLMAdapter('custom_llm', 'your-api-key')
      orchestrator.register_module('topology', TopologyAdapter())
      # 注入自定义 LLM
      orchestrator.modules['topology'].service.llm_client = custom_llm.client

      results = orchestrator.run_pipeline()
      print("Pipeline completed with custom modules!")

  if __name__ == '__main__':
      main()


  # examples/custom_module.py
  """
  自定义模块示例 - 演示如何创建新模块
  """

  from core.module_adapter import ModuleAdapter
  from common.schemas import BaseModel
  from typing import Dict, List

  # 定义新模块的输出格式
  class CustomStageOutput(BaseModel):
      """自定义阶段输出"""
      processed_data: List[Dict]
      custom_metrics: Dict
      metadata: Dict

  # 实现自定义模块
  class CustomProcessingAdapter(ModuleAdapter):
      """
      自定义处理模块 - 可以插入到任何阶段之间
      """

      def __init__(self):
          self.processor = None

      def process(self, input_data: Dict, config: Dict) -> CustomStageOutput:
          """
          自定义处理逻辑
          例如：数据清洗、格式转换、质量检查等
          """
          print("[Custom] Processing data with custom logic...")

          # 提取输入数据
          data = input_data.get('data', [])

          # 应用自定义处理
          processed_data = []
          for item in data:
              processed_item = self._custom_transform(item, config)
              processed_data.append(processed_item)

          # 计算自定义指标
          custom_metrics = self._calculate_metrics(processed_data)

          return CustomStageOutput(
              processed_data=processed_data,
              custom_metrics=custom_metrics,
              metadata={
                  'stage': 'custom_processing',
                  'version': '1.0.0',
                  'config': config
              }
          )

      def _custom_transform(self, item: Dict, config: Dict) -> Dict:
          """自定义转换逻辑"""
          # 实现具体的转换逻辑
          transformed = item.copy()

          # 示例：添加额外字段
          transformed['processed'] = True
          transformed['quality_score'] = self._calculate_quality(item)

          return transformed

      def _calculate_quality(self, item: Dict) -> float:
          """计算质量分数"""
          # 实现质量评估逻辑
          return 0.85

      def _calculate_metrics(self, data: List[Dict]) -> Dict:
          """计算指标"""
          return {
              'total_items': len(data),
              'average_quality': sum(d.get('quality_score', 0) for d in data) / len(data) if data else 0,
              'processed_count': sum(1 for d in data if d.get('processed', False))
          }

      def validate_input(self, input_data: Dict) -> bool:
          """验证输入"""
          return 'data' in input_data or isinstance(input_data, list)

      def get_metadata(self) -> Dict:
          return {
              'name': 'Custom Processing',
              'version': '1.0.0',
              'description': 'Custom data processing module',
              'input_format': 'any',
              'output_format': 'custom_stage_output'
          }

  # 使用自定义模块
  def use_custom_module():
      from core.pipeline_orchestrator import PipelineOrchestrator

      # 修改配置文件，添加自定义阶段
      config = {
          'stages': {
              'normalization': {...},
              'custom_processing': {  # 新增自定义阶段
                  'module': 'custom_module.CustomProcessingAdapter',
                  'enabled': True,
                  'config': {
                      'quality_threshold': 0.8
                  },
                  'input': {
                      'type': 'file',
                      'path': 'output/stage1/capabilities.json'
                  },
                  'output': {
                      'type': 'file',
                      'path': 'output/custom/processed.json'
                  }
              },
              'topology': {...}
          }
      }

      orchestrator = PipelineOrchestrator(config)
      orchestrator.register_module('custom_processing', CustomProcessingAdapter())

      results = orchestrator.run_pipeline()
      return results

  十、监控与日志

  # core/monitoring.py
  """
  监控模块 - 跟踪流程执行状态和性能
  """

  import time
  import logging
  from typing import Dict, List
  from datetime import datetime
  import json

  class PipelineMonitor:
      """流程监控器"""

      def __init__(self, log_file: str = 'pipeline.log'):
          self.log_file = log_file
          self.metrics = {}
          self.stage_timings = {}

          # 配置日志
          logging.basicConfig(
              level=logging.INFO,
              format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
              handlers=[
                  logging.FileHandler(log_file),
                  logging.StreamHandler()
              ]
          )
          self.logger = logging.getLogger('PipelineMonitor')

      def start_stage(self, stage_name: str):
          """开始阶段"""
          self.stage_timings[stage_name] = {
              'start_time': time.time(),
              'status': 'running'
          }
          self.logger.info(f"Stage '{stage_name}' started")

      def end_stage(self, stage_name: str, success: bool = True, error: str = None):
          """结束阶段"""
          if stage_name in self.stage_timings:
              end_time = time.time()
              start_time = self.stage_timings[stage_name]['start_time']
              duration = end_time - start_time

              self.stage_timings[stage_name].update({
                  'end_time': end_time,
                  'duration': duration,
                  'status': 'success' if success else 'failed',
                  'error': error
              })

              if success:
                  self.logger.info(f"Stage '{stage_name}' completed in {duration:.2f}s")
              else:
                  self.logger.error(f"Stage '{stage_name}' failed: {error}")

      def record_metric(self, stage_name: str, metric_name: str, value: float):
          """记录指标"""
          if stage_name not in self.metrics:
              self.metrics[stage_name] = {}
          self.metrics[stage_name][metric_name] = value

          self.logger.info(f"[{stage_name}] {metric_name}: {value}")

      def get_summary(self) -> Dict:
          """获取执行摘要"""
          total_duration = sum(
              timing.get('duration', 0)
              for timing in self.stage_timings.values()
          )

          failed_stages = [
              name for name, timing in self.stage_timings.items()
              if timing.get('status') == 'failed'
          ]

          return {
              'total_duration': total_duration,
              'total_stages': len(self.stage_timings),
              'successful_stages': len(self.stage_timings) - len(failed_stages),
              'failed_stages': failed_stages,
              'stage_timings': self.stage_timings,
              'metrics': self.metrics
          }

      def export_report(self, output_path: str):
          """导出报告"""
          summary = self.get_summary()

          with open(output_path, 'w', encoding='utf-8') as f:
              json.dump(summary, f, indent=2, ensure_ascii=False)

          self.logger.info(f"Report exported to {output_path}")


  # 集成到 PipelineOrchestrator
  class MonitoredPipelineOrchestrator(PipelineOrchestrator):
      """带监控的流程编排器"""

      def __init__(self, config_path: str):
          super().__init__(config_path)
          self.monitor = PipelineMonitor()

      def _execute_stage(self, stage_name: str, stage_config: Dict):
          """执行单个阶段（带监控）"""
          self.monitor.start_stage(stage_name)

          try:
              result = super()._execute_stage(stage_name, stage_config)

              # 记录指标
              if hasattr(result, 'metadata'):
                  for key, value in result.metadata.items():
                      if isinstance(value, (int, float)):
                          self.monitor.record_metric(stage_name, key, value)

              self.monitor.end_stage(stage_name, success=True)
              return result

          except Exception as e:
              self.monitor.end_stage(stage_name, success=False, error=str(e))
              raise

      def run_pipeline(self, start_stage=None, end_stage=None):
          """运行流程（带监控）"""
          try:
              results = super().run_pipeline(start_stage, end_stage)

              # 导出报告
              self.monitor.export_report('output/pipeline_report.json')

              return results

          except Exception as e:
              self.monitor.logger.error(f"Pipeline failed: {str(e)}")
              self.monitor.export_report('output/pipeline_report_failed.json')
              raise

  十一、错误处理与重试

  # core/error_handler.py
  """
  错误处理模块 - 提供重试、回滚等机制
  """

  import time
  from typing import Callable, Any, Optional
  from functools import wraps

  class RetryConfig:
      """重试配置"""
      def __init__(self, max_retries: int = 3, delay: float = 1.0, backoff: float = 2.0):
          self.max_retries = max_retries
          self.delay = delay
          self.backoff = backoff

  def retry_on_failure(config: RetryConfig):
      """重试装饰器"""
      def decorator(func: Callable) -> Callable:
          @wraps(func)
          def wrapper(*args, **kwargs) -> Any:
              last_exception = None
              delay = config.delay

              for attempt in range(config.max_retries + 1):
                  try:
                      return func(*args, **kwargs)
                  except Exception as e:
                      last_exception = e

                      if attempt < config.max_retries:
                          print(f"Attempt {attempt + 1} failed: {str(e)}")
                          print(f"Retrying in {delay}s...")
                          time.sleep(delay)
                          delay *= config.backoff
                      else:
                          print(f"All {config.max_retries + 1} attempts failed")

              raise last_exception

          return wrapper
      return decorator


  class PipelineCheckpoint:
      """流程检查点 - 支持断点续传"""

      def __init__(self, checkpoint_dir: str = 'output/checkpoints'):
          self.checkpoint_dir = checkpoint_dir
          import os
          os.makedirs(checkpoint_dir, exist_ok=True)

      def save_checkpoint(self, stage_name: str, result: Dict):
          """保存检查点"""
          import json
          checkpoint_path = f"{self.checkpoint_dir}/{stage_name}.json"

          with open(checkpoint_path, 'w', encoding='utf-8') as f:
              json.dump({
                  'stage': stage_name,
                  'timestamp': time.time(),
                  'result': result
              }, f, indent=2, ensure_ascii=False)

          print(f"Checkpoint saved: {checkpoint_path}")

      def load_checkpoint(self, stage_name: str) -> Optional[Dict]:
          """加载检查点"""
          import json
          import os

          checkpoint_path = f"{self.checkpoint_dir}/{stage_name}.json"

          if not os.path.exists(checkpoint_path):
              return None

          with open(checkpoint_path, 'r', encoding='utf-8') as f:
              checkpoint = json.load(f)

          print(f"Checkpoint loaded: {checkpoint_path}")
          return checkpoint.get('result')

      def clear_checkpoints(self):
          """清除所有检查点"""
          import os
          import shutil

          if os.path.exists(self.checkpoint_dir):
              shutil.rmtree(self.checkpoint_dir)
              os.makedirs(self.checkpoint_dir)

          print("All checkpoints cleared")


  class ResilientPipelineOrchestrator(PipelineOrchestrator):
      """具有容错能力的流程编排器"""

      def __init__(self, config_path: str, enable_checkpoints: bool = True):
          super().__init__(config_path)
          self.checkpoint = PipelineCheckpoint() if enable_checkpoints else None
          self.retry_config = RetryConfig(max_retries=3, delay=2.0)

      def _execute_stage(self, stage_name: str, stage_config: Dict):
          """执行单个阶段（带重试和检查点）"""

          # 尝试从检查点恢复
          if self.checkpoint:
              cached_result = self.checkpoint.load_checkpoint(stage_name)
              if cached_result:
                  print(f"[{stage_name}] Restored from checkpoint")
                  return cached_result

          # 使用重试机制执行
          @retry_on_failure(self.retry_config)
          def execute_with_retry():
              return super(ResilientPipelineOrchestrator, self)._execute_stage(
                  stage_name, stage_config
              )

          try:
              result = execute_with_retry()

              # 保存检查点
              if self.checkpoint:
                  self.checkpoint.save_checkpoint(stage_name, result)

              return result

          except Exception as e:
              print(f"[{stage_name}] Failed after all retries: {str(e)}")
              raise

  十二、配置管理

  # core/config_manager.py
  """
  配置管理模块 - 支持多环境配置、配置验证等
  """

  import yaml
  import os
  from typing import Dict, Any
  from pathlib import Path

  class ConfigManager:
      """配置管理器"""

      def __init__(self, config_dir: str = 'config'):
          self.config_dir = Path(config_dir)
          self.configs = {}
          self.environment = os.getenv('FLORA_ENV', 'development')

      def load_config(self, config_name: str) -> Dict:
          """
          加载配置文件
          支持环境特定配置覆盖
          """
          # 加载基础配置
          base_config_path = self.config_dir / f"{config_name}.yaml"
          base_config = self._load_yaml(base_config_path)

          # 加载环境特定配置
          env_config_path = self.config_dir / f"{config_name}.{self.environment}.yaml"
          if env_config_path.exists():
              env_config = self._load_yaml(env_config_path)
              base_config = self._merge_configs(base_config, env_config)

          # 替换环境变量
          base_config = self._replace_env_vars(base_config)

          self.configs[config_name] = base_config
          return base_config

      def _load_yaml(self, path: Path) -> Dict:
          """加载 YAML 文件"""
          with open(path, 'r', encoding='utf-8') as f:
              return yaml.safe_load(f)

      def _merge_configs(self, base: Dict, override: Dict) -> Dict:
          """合并配置（深度合并）"""
          result = base.copy()

          for key, value in override.items():
              if key in result and isinstance(result[key], dict) and isinstance(value, dict):
                  result[key] = self._merge_configs(result[key], value)
              else:
                  result[key] = value

          return result

      def _replace_env_vars(self, config: Any) -> Any:
          """替换环境变量"""
          if isinstance(config, dict):
              return {k: self._replace_env_vars(v) for k, v in config.items()}
          elif isinstance(config, list):
              return [self._replace_env_vars(item) for item in config]
          elif isinstance(config, str) and config.startswith('${') and config.endswith('}'):
              env_var = config[2:-1]
              return os.getenv(env_var, config)
          else:
              return config

      def validate_config(self, config: Dict, schema: Dict) -> bool:
          """
          验证配置格式
          Args:
              config: 配置字典
              schema: 验证模式
          """
          from jsonschema import validate, ValidationError

          try:
              validate(instance=config, schema=schema)
              return True
          except ValidationError as e:
              print(f"Config validation failed: {e.message}")
              return False

      def get_config(self, config_name: str) -> Dict:
          """获取配置（带缓存）"""
          if config_name not in self.configs:
              return self.load_config(config_name)
          return self.configs[config_name]


  # 配置验证模式
  PIPELINE_CONFIG_SCHEMA = {
      "type": "object",
      "properties": {
          "pipeline": {
              "type": "object",
              "properties": {
                  "name": {"type": "string"},
                  "version": {"type": "string"}
              },
              "required": ["name", "version"]
          },
          "stages": {
              "type": "object",
              "patternProperties": {
                  ".*": {
                      "type": "object",
                      "properties": {
                          "module": {"type": "string"},
                          "enabled": {"type": "boolean"},
                          "config": {"type": "object"},
                          "input": {"type": "object"},
                          "output": {"type": "object"}
                      },
                      "required": ["module", "input", "output"]
                  }
              }
          }
      },
      "required": ["pipeline", "stages"]
  }


  # 多环境配置示例
  # config/pipeline_config.yaml (基础配置)
  """
  pipeline:
    name: "Flora-Evaluation-Pipeline"
    version: "1.0.0"

  stages:
    normalization:
      module: "api_normalization.NormalizationService"
      enabled: true
      config:
        use_hdbscan: true
      input:
        type: "file"
        path: "input/swagger.json"
      output:
        type: "file"
        path: "output/stage1/capabilities.json"
  """

  # config/pipeline_config.production.yaml (生产环境覆盖)
  """
  stages:
    normalization:
      config:
        use_hdbscan: true
        enable_evaluation: true
        cache_enabled: true

    topology:
      config:
        neo4j_uri: "bolt://prod-neo4j:7687"
        connection_pool_size: 50
  """

  # config/pipeline_config.development.yaml (开发环境覆盖)
  """
  stages:
    normalization:
      config:
        use_hdbscan: false  # 开发环境使用更快的算法
        enable_evaluation: false

    topology:
      config:
        neo4j_uri: "bolt://localhost:7687"
  """

  十三、CLI 工具

  # cli.py
  """
  命令行工具 - 提供便捷的命令行接口
  """

  import click
  import os
  from pathlib import Path
  from core.pipeline_orchestrator import ResilientPipelineOrchestrator
  from core.config_manager import ConfigManager
  from adapters import *

  @click.group()
  @click.version_option(version='1.0.0')
  def cli():
      """Flora-Evaluation Pipeline CLI"""
      pass

  @cli.command()
  @click.option('--config', '-c', default='config/pipeline_config.yaml', help='配置文件路径')
  @click.option('--env', '-e', default='development', help='运行环境 (development/production)')
  @click.option('--start', '-s', default=None, help='起始阶段')
  @click.option('--end', '-E', default=None, help='结束阶段')
  @click.option('--checkpoint/--no-checkpoint', default=True, help='是否启用检查点')
  def run(config, env, start, end, checkpoint):
      """运行完整流程或部分流程"""

      # 设置环境
      os.environ['FLORA_ENV'] = env

      click.echo(f"🚀 Starting Flora-Evaluation Pipeline")
      click.echo(f"   Environment: {env}")
      click.echo(f"   Config: {config}")

      if start or end:
          click.echo(f"   Range: {start or 'start'} -> {end or 'end'}")

      # 创建编排器
      orchestrator = ResilientPipelineOrchestrator(config, enable_checkpoints=checkpoint)

      # 注册所有模块
      _register_modules(orchestrator)

      # 运行流程
      try:
          results = orchestrator.run_pipeline(start_stage=start, end_stage=end)

          click.echo("\n✅ Pipeline completed successfully!")
          click.echo(f"   Total stages: {len(results)}")

          # 显示摘要
          if orchestrator.monitor:
              summary = orchestrator.monitor.get_summary()
              click.echo(f"   Total duration: {summary['total_duration']:.2f}s")
              click.echo(f"   Report: output/pipeline_report.json")

      except Exception as e:
          click.echo(f"\n❌ Pipeline failed: {str(e)}", err=True)
          raise click.Abort()

  @cli.command()
  @click.argument('stage_name')
  @click.option('--config', '-c', default='config/pipeline_config.yaml', help='配置文件路径')
  def run_stage(stage_name, config):
      """运行单个阶段"""

      click.echo(f"🔧 Running stage: {stage_name}")

      orchestrator = ResilientPipelineOrchestrator(config)
      _register_modules(orchestrator)

      try:
          results = orchestrator.run_pipeline(start_stage=stage_name, end_stage=stage_name)
          click.echo(f"✅ Stage '{stage_name}' completed!")

      except Exception as e:
          click.echo(f"❌ Stage '{stage_name}' failed: {str(e)}", err=True)
          raise click.Abort()

  @cli.command()
  @click.option('--config', '-c', default='config/pipeline_config.yaml', help='配置文件路径')
  def list_stages(config):
      """列出所有可用阶段"""

      config_manager = ConfigManager()
      pipeline_config = config_manager.load_config('pipeline_config')

      click.echo("📋 Available stages:\n")

      for stage_name, stage_config in pipeline_config['stages'].items():
          enabled = "✓" if stage_config.get('enabled', True) else "✗"
          module = stage_config['module']

          click.echo(f"  [{enabled}] {stage_name}")
          click.echo(f"      Module: {module}")
          click.echo(f"      Input: {stage_config['input']['type']}")
          click.echo(f"      Output: {stage_config['output']['type']}")
          click.echo()

  @cli.command()
  @click.option('--stage', '-s', default=None, help='清除特定阶段的检查点')
  def clear_checkpoints(stage):
      """清除检查点"""

      from core.error_handler import PipelineCheckpoint

      checkpoint = PipelineCheckpoint()

      if stage:
          import os
          checkpoint_path = f"{checkpoint.checkpoint_dir}/{stage}.json"
          if os.path.exists(checkpoint_path):
              os.remove(checkpoint_path)
              click.echo(f"✅ Checkpoint for '{stage}' cleared")
          else:
              click.echo(f"⚠️  No checkpoint found for '{stage}'")
      else:
          checkpoint.clear_checkpoints()
          click.echo("✅ All checkpoints cleared")

  @cli.command()
  @click.argument('input_file')
  @click.option('--output', '-o', default='output/validation_report.json', help='输出报告路径')
  def validate(input_file, output):
      """验证输入文件格式"""

      import json
      from common.schemas import Stage1Output, Stage2Output, Stage3AOutput, Stage3BOutput

      click.echo(f"🔍 Validating: {input_file}")

      try:
          with open(input_file, 'r', encoding='utf-8') as f:
              data = json.load(f)

          # 尝试识别数据类型并验证
          if 'capabilities' in data:
              Stage1Output(**data)
              click.echo("✅ Valid Stage 1 output (API Normalization)")
          elif 'dependencies' in data:
              Stage2Output(**data)
              click.echo("✅ Valid Stage 2 output (API Topology)")
          elif 'entity_mappings' in data:
              Stage3AOutput(**data)
              click.echo("✅ Valid Stage 3A output (Database Mapping)")
          elif 'scenarios' in data:
              Stage3BOutput(**data)
              click.echo("✅ Valid Stage 3B output (Scenario Generation)")
          else:
              click.echo("⚠️  Unknown data format", err=True)

      except Exception as e:
          click.echo(f"❌ Validation failed: {str(e)}", err=True)
          raise click.Abort()

  @cli.command()
  @click.option('--output', '-o', default='output/status.json', help='输出状态文件')
  def status(output):
      """查看流程状态"""

      from core.error_handler import PipelineCheckpoint
      import json

      checkpoint = PipelineCheckpoint()

      click.echo("📊 Pipeline Status:\n")

      # 检查各阶段的检查点
      stages = [
          'normalization', 'topology', 'database_mapping',
          'scenario_generation', 'agent_build', 'mock_service',
          'evaluation', 'optimization'
      ]

      status_info = {}

      for stage in stages:
          checkpoint_data = checkpoint.load_checkpoint(stage)

          if checkpoint_data:
              click.echo(f"  ✓ {stage}: Completed")
              status_info[stage] = 'completed'
          else:
              click.echo(f"  ○ {stage}: Not started")
              status_info[stage] = 'not_started'

      # 保存状态
      with open(output, 'w', encoding='utf-8') as f:
          json.dump(status_info, f, indent=2)

      click.echo(f"\n📄 Status saved to: {output}")

  @cli.command()
  @click.option('--max-iterations', '-m', default=5, help='最大优化迭代次数')
  @click.option('--config', '-c', default='config/pipeline_config.yaml', help='配置文件路径')
  def optimize(max_iterations, config):
      """运行优化循环"""

      click.echo(f"🔄 Starting optimization loop (max {max_iterations} iterations)")

      orchestrator = ResilientPipelineOrchestrator(config)
      _register_modules(orchestrator)

      iteration = 1
      previous_success_rate = 0.0

      while iteration <= max_iterations:
          click.echo(f"\n--- Iteration {iteration}/{max_iterations} ---")

          # 运行评估和优化
          results = orchestrator.run_pipeline(
              start_stage='evaluation',
              end_stage='optimization'
          )

          # 检查是否需要继续
          opt_result = results['optimization']
          eval_result = results['evaluation']

          current_success_rate = eval_result.success_rate

          click.echo(f"Success rate: {current_success_rate:.2%}")

          if not opt_result.should_rebuild:
              click.echo("✅ Optimization converged!")
              break

          if current_success_rate <= previous_success_rate:
              click.echo("⚠️  No improvement, stopping optimization")
              break

          improvement = current_success_rate - previous_success_rate
          click.echo(f"📈 Improvement: +{improvement:.2%}")

          previous_success_rate = current_success_rate
          iteration += 1

      click.echo(f"\n✅ Optimization completed after {iteration} iterations")

  @cli.command()
  @click.option('--format', '-f', type=click.Choice(['json', 'yaml', 'markdown']), default='json', help='导出格式')
  @click.option('--output', '-o', default='output/export', help='导出目录')
  def export(format, output):
      """导出流程结果"""

      import json
      import yaml
      from pathlib import Path

      output_dir = Path(output)
      output_dir.mkdir(parents=True, exist_ok=True)

      click.echo(f"📦 Exporting results to: {output_dir}")

      # 收集所有阶段的输出
      stages = {
          'stage1': 'output/stage1/capabilities.json',
          'stage2': 'output/stage2/topology.json',
          'stage3a': 'output/stage3a/entity_mapping.json',
          'stage3b': 'output/stage3b/scenarios.json',
          'stage4a': 'output/stage4a/agent_system.json',
          'stage5': 'output/stage5/evaluation_results.json',
          'stage6': 'output/stage6/optimization.json'
      }

      for stage_name, stage_path in stages.items():
          if Path(stage_path).exists():
              with open(stage_path, 'r', encoding='utf-8') as f:
                  data = json.load(f)

              if format == 'json':
                  export_path = output_dir / f"{stage_name}.json"
                  with open(export_path, 'w', encoding='utf-8') as f:
                      json.dump(data, f, indent=2, ensure_ascii=False)

              elif format == 'yaml':
                  export_path = output_dir / f"{stage_name}.yaml"
                  with open(export_path, 'w', encoding='utf-8') as f:
                      yaml.dump(data, f, allow_unicode=True)

              elif format == 'markdown':
                  export_path = output_dir / f"{stage_name}.md"
                  with open(export_path, 'w', encoding='utf-8') as f:
                      f.write(_convert_to_markdown(stage_name, data))

              click.echo(f"  ✓ Exported: {export_path}")

      click.echo("\n✅ Export completed!")

  def _register_modules(orchestrator):
      """注册所有模块"""
      orchestrator.register_module('normalization', NormalizationAdapter())
      orchestrator.register_module('topology', TopologyAdapter())
      orchestrator.register_module('database_mapping', EntiMapAdapter())
      orchestrator.register_module('scenario_generation', ScenarioAdapter())
      orchestrator.register_module('agent_build', AgentBuildAdapter())
      orchestrator.register_module('mock_service', MockAdapter())
      orchestrator.register_module('evaluation', EvaluationAdapter())
      orchestrator.register_module('optimization', OptimizationAdapter())

  def _convert_to_markdown(stage_name: str, data: dict) -> str:
      """将数据转换为 Markdown 格式"""
      md = f"# {stage_name.upper()} Results\n\n"

      # 添加元数据
      if 'metadata' in data:
          md += "## Metadata\n\n"
          for key, value in data['metadata'].items():
              md += f"- **{key}**: {value}\n"
          md += "\n"

      # 添加统计信息
      if 'statistics' in data:
          md += "## Statistics\n\n"
          for key, value in data['statistics'].items():
              md += f"- **{key}**: {value}\n"
          md += "\n"

      return md

  if __name__ == '__main__':
      cli()

  十四、使用文档

  # Flora-Evaluation 使用指南

  ## 快速开始

  ### 1. 安装依赖

  ```bash
  pip install -r requirements.txt

  1. 配置环境变量

  # .env 文件
  NEO4J_PASSWORD=your-neo4j-password
  DASHSCOPE_API_KEY=your-dashscope-api-key
  DATABASE_URL=mysql://user:pass@host:port/db
  OPTIMIZATION_API=https://your-optimization-api.com

  3. 运行完整流程

  # 使用 CLI
  python cli.py run --config config/pipeline_config.yaml --env production

  # 或使用 Python
  python main.py

  CLI 命令

  运行流程

  # 运行完整流程
  python cli.py run

  # 运行部分流程
  python cli.py run --start normalization --end topology

  # 运行单个阶段
  python cli.py run-stage normalization

  # 指定环境
  python cli.py run --env production

  管理检查点

  # 查看状态
  python cli.py status

  # 清除所有检查点
  python cli.py clear-checkpoints

  # 清除特定阶段检查点
  python cli.py clear-checkpoints --stage normalization

  验证和导出

  # 验证输入文件
  python cli.py validate input/swagger.json

  # 导出结果
  python cli.py export --format json --output output/export

  # 列出所有阶段
  python cli.py list-stages

  优化循环

  # 运行优化循环
  python cli.py optimize --max-iterations 5

  模块替换示例

  替换 API 规范化模块

  # custom_normalization.py
  from core.module_adapter import ModuleAdapter

  class CustomNormalizationAdapter(ModuleAdapter):
      def process(self, input_data, config):
          # 自定义实现
          pass

  # 使用
  orchestrator.register_module('normalization', CustomNormalizationAdapter())

  替换 LLM 提供商

  # 使用通义千问
  from core.llm_factory import LLMFactory

  llm_client = LLMFactory.create_client('qwen', api_key='your-key')

  # 注入到模块
  topology_adapter = TopologyAdapter()
  topology_adapter.service.llm_client = llm_client

  替换存储后端

  # 使用 Milvus 替代 Neo4j
  from core.storage_factory import StorageFactory

  storage = StorageFactory.create_graph_store('milvus',
      host='localhost',
      port=19530
  )

  配置文件说明

  基础配置 (pipeline_config.yaml)

  pipeline:
    name: "Flora-Evaluation-Pipeline"
    version: "1.0.0"

  stages:
    normalization:
      module: "api_normalization.NormalizationService"
      enabled: true
      config:
        use_hdbscan: true
        use_prance: true
      input:
        type: "file"
        path: "input/swagger.json"
      output:
        type: "file"
        path: "output/stage1/capabilities.json"

  环境特定配置

  - pipeline_config.development.yaml - 开发环境
  - pipeline_config.production.yaml - 生产环境
  - pipeline_config.test.yaml - 测试环境

  故障排除

  问题: Neo4j 连接失败

  # 检查 Neo4j 是否运行
  docker ps | grep neo4j

  # 重启 Neo4j
  docker restart neo4j

  问题: LLM API 调用失败

  # 检查 API Key
  echo $DASHSCOPE_API_KEY

  # 测试 API 连接
  curl -H "Authorization: Bearer $DASHSCOPE_API_KEY" \
    https://dashscope.aliyuncs.com/compatible-mode/v1/models

  问题: 内存不足

  # 启用检查点以支持断点续传
  python cli.py run --checkpoint

  # 分阶段运行
  python cli.py run --start normalization --end normalization
  python cli.py run --start topology --end topology

  性能优化建议

  1. 使用检查点: 避免重复计算
  2. 并行执行: 分支 A 和分支 B 可以并行运行
  3. 缓存 LLM 结果: 减少 API 调用次数
  4. 批量处理: 批量创建 Neo4j 节点和关系
  5. 资源限制: 限制并发数和内存使用

  监控和日志

  查看日志

  # 实时查看日志
  tail -f pipeline.log

  # 查看特定阶段日志
  grep "normalization" pipeline.log

  查看报告

  # 查看执行报告
  cat output/pipeline_report.json

  # 查看监控指标
  cat output/metrics.json

  最佳实践

  1. 先小规模测试: 使用少量 API 测试流程
  2. 逐步启用功能: 先禁用 LLM，验证基础功能
  3. 定期备份: 备份 Neo4j 数据和输出文件
  4. 版本控制配置: 将配置文件纳入版本控制
  5. 监控资源使用: 关注内存和 CPU 使用情况

  ## 十五、总结

  这个模块化架构设计的核心要点：

  ### 1. **清晰的分层**
  - 6 个主要阶段，每个阶段职责明确
  - 分支 A（构建）和分支 B（测试）可独立运行
  - Stage 6 形成闭环优化

  ### 2. **标准化接口**
  - 所有模块实现 `ModuleAdapter` 接口
  - 统一的数据格式（Pydantic Models）
  - 标准的输入/输出规范

  ### 3. **易于替换**
  - 模块注册表机制
  - LLM 工厂模式
  - 存储后端工厂模式
  - 配置驱动的模块加载

  ### 4. **容错机制**
  - 自动重试
  - 检查点/断点续传
  - 错误处理和日志

  ### 5. **可观测性**
  - 完整的监控系统
  - 详细的日志记录
  - 性能指标收集
  - 执行报告生成

  ### 6. **便捷的工具**
  - CLI 命令行工具
  - 多环境配置支持
  - 验证和导出功能

  这个设计可以让你：
  - ✅ 轻松替换任何模块（规范化、拓扑、LLM、存储等）
  - ✅ 独立运行任何阶段或分支
  - ✅ 支持断点续传和错误恢复
  - ✅ 方便地进行测试和调试
  - ✅ 适应不同的部署环境
  - ✅ 持续优化和迭代
